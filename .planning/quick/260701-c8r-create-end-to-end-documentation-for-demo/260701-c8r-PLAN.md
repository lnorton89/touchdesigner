---
status: in_progress
date: 2026-07-01
---

# Quick Task 260701-c8r: Demo End-to-End Documentation And Controls

## Goal

Make the generated TouchDesigner demo easier to use without reading source code:

- Document the end-to-end process from dependencies through router, agent, tools, and MCP bridge.
- Document every node currently generated into `base_llm_demo`, including its purpose and whether users should touch it.
- Remove or clarify generated nodes that are only internal scaffolding.
- Add relevant controls to the main demo component where feasible from the generator.

## Plan

1. Update `scripts/toe_builder.py` so the generated `.toe` contains first-class in-project docs and control entry points.
2. Add runtime helper text/scripts that install demo-level custom parameters when TouchDesigner supports `appendCustomPage`.
3. Update `demo/README.md` with an end-to-end workflow and full node reference.
4. Update top-level docs and `.tox` structure notes so they match the current router, agent, tool registry, and MCP demo shape.
5. Regenerate `demo/demo.toe` from source and run Python tests.

## Notes

- Existing uncommitted changes were present before this task. Preserve them and avoid broad cleanup.
- TouchDesigner expanded-file conventions from `AGENTS.md` apply if `.toe` internals are touched.
