---
phase: 01-async-router-proof
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - scripts/toe_builder.py
  - demo/demo.toe
  - td_components/llm_model_router/ModelRouterExt.py
  - td_components/llm_model_router/router_callbacks.py
  - tests/test_router_payloads.py
autonomous: false
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-03
  - ROUT-05
  - ROUT-06
  - RUNT-01
  - RUNT-02
  - RUNT-03
  - EXMP-01
  - EXMP-03
  - EXMP-04
must_haves:
  truths:
    - The generated `demo.toe` must include a runnable Model Router surface, not only the MCP bridge smoke surface.
    - TouchDesigner runtime verification must be possible before Phase 4 packaging; Phase 4 can make the operator reusable as `.tox`, but Phase 1 still needs a generated demo network that exercises the Router.
    - The Router must expose observable TD-friendly response, status, error, callback, reset, retry, and nonblocking behavior from inside the generated demo.
  artifacts:
    - scripts/toe_builder.py
    - demo/demo.toe
    - td_components/llm_model_router/ModelRouterExt.py
    - tests/test_router_payloads.py
  key_links:
    - UAT currently blocks because no generated TD Router surface exists to verify.
    - The MCP bridge now proves safe HTTP-thread-to-TD-main-thread handoff, but that is not a substitute for Phase 1 Router UAT.
---

<objective>
Close the Phase 1 runtime verification gap by making `demo/demo.toe` exercise the Model Router itself.

Purpose: unblock the eight Phase 1 UAT checks without waiting for Phase 4 `.tox` packaging.
Output: regenerated demo project with a runnable Router COMP/demo surface, TD-visible outputs, triggers, reset/retry controls, and tests/docs that prove the surface can be smoke-tested.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-async-router-proof/01-CONTEXT.md
@.planning/phases/01-async-router-proof/01-UAT.md
@.planning/phases/01-async-router-proof/01-VERIFICATION.md
@.planning/phases/01-async-router-proof/01-03-SUMMARY.md
@td_components/llm_model_router/ModelRouterExt.py
@scripts/toe_builder.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate a real Router demo network</name>
  <files>scripts/toe_builder.py, demo/demo.toe</files>
  <action>Extend the demo generator so `demo.toe` contains a usable `llm_model_router` Base COMP with visible Router configuration, prompt input, response text, status JSON, error text, callback payload, and status channel evidence. Keep the MCP bridge smoke test if useful, but do not let it be the only live behavior in the demo. Avoid user-specific saved paths except the current development path needed for local source loading in the generated test project; document any such bootstrap assumption for Phase 4 removal.</action>
  <verify>
    <automated>python scripts/generate-demo-toe.py</automated>
    <automated>python -m pytest -q</automated>
    <human-check>Open `demo/demo.toe` in TouchDesigner and confirm the Router demo operators are present under `/project1/base_llm_demo`.</human-check>
  </verify>
  <done>`demo.toe` opens and exposes a Model Router demo surface that can be inspected through TD and through the MCP bridge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Router output mutation for TD-visible DAT state</name>
  <files>td_components/llm_model_router/ModelRouterExt.py, tests/test_router_payloads.py</files>
  <behavior>
    - Response text is written to the configured response DAT.
    - Error text and structured status are written to visible DATs.
    - Runtime state still updates under normal Python tests without TD imports.
  </behavior>
  <action>Implement the previously stubbed `_write_outputs()` boundary enough for the generated demo to show response text, error text, and status JSON. Use owner-relative operator lookup when available; keep defensive fallbacks so unit tests can exercise state without real TD operators. Do not move network calls onto the TD main thread.</action>
  <verify>
    <automated>python -m pytest -q</automated>
  </verify>
  <done>Router state changes are externally visible in DAT-friendly form and existing payload/state tests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Add a runtime smoke probe for the generated demo</name>
  <files>scripts/toe_builder.py, tests/test_mcp_bridge.py</files>
  <action>Add or update a small verification path that can launch the generated demo, wait for startup, inspect key operators through the MCP bridge, and confirm TouchDesigner remains running. Keep the probe read-only by default. Record the exact commands in the summary so UAT can be repeated.</action>
  <verify>
    <automated>python -m pytest -q</automated>
    <human-check>Launch `demo.toe`, wait 20 seconds, read the Router demo DATs/children through the bridge, and confirm the TD process is still responding.</human-check>
  </verify>
  <done>The generated demo has a repeatable smoke test that verifies open, bridge communication, and process survival.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Re-run Phase 1 UAT</name>
  <files>.planning/phases/01-async-router-proof/01-UAT.md, .planning/phases/01-async-router-proof/01-VERIFICATION.md</files>
  <action>After the Router demo surface is implemented, run the eight Phase 1 UAT checks in TouchDesigner and update `01-UAT.md` plus `01-VERIFICATION.md` from blocked/stale to passed or to concrete issues.</action>
  <how-to-verify>
    1. Confirm Router custom parameters/config surface exists.
    2. Pulse trigger starts a request and status becomes running immediately.
    3. Frame counter or TD responsiveness continues during a slow request.
    4. DAT/table-change trigger calls the central Router path.
    5. Local OpenAI-compatible endpoint writes response text.
    6. Unreachable endpoint or short timeout writes recoverable error state.
    7. Callback target receives structured payload fields.
    8. Reset and retry behave visibly without losing configuration.
  </how-to-verify>
  <verify>
    <automated>python -m pytest -q</automated>
    <human-check>All eight UAT checks are recorded with definitive pass/issue/skipped-with-reason results.</human-check>
  </verify>
  <done>Phase 1 verification is no longer stale or blocked by missing runtime demo surface.</done>
  <resume-signal>Type "approved" or describe the failing UAT step.</resume-signal>
</task>

</tasks>

<verification>
Run `python -m pytest -q`, regenerate `demo/demo.toe`, open it in TouchDesigner 2022.25370, wait 20 seconds, perform read-only bridge inspection, and complete the eight Phase 1 UAT checks.
</verification>

<success_criteria>
Plan 04 is complete when Phase 1 UAT can be run against `demo/demo.toe` without Phase 4 packaging, and each Router behavior has a definitive result in `01-UAT.md`.
</success_criteria>

<output>
Create `.planning/phases/01-async-router-proof/01-04-SUMMARY.md` when done.
</output>
