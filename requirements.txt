# External virtual environment dependencies for TouchDesigner LLM operators.
# Install with: .\scripts\bootstrap-venv.ps1
# Python target: 3.11 (match TouchDesigner's embedded version)

httpx>=0.27,<1
pydantic>=2.5,<3
pydantic-settings>=2.1,<3
